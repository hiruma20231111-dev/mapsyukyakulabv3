# マップ集客ラボ V3 — 実装計画（plan.md）

> V3開発の「憲法」。迷ったらここに戻る。作成日: 2026-09-16 ／ リポジトリ: `hiruma20231111-dev/mapsyukyakulabv3`
> 起点: `maplab_v2`（features/design/lib のレイヤー構成をそのまま土台）。本ファイルはV2からの**差分（デルタ）**を定義する。

---

## 0. V3のコンセプト（一言）

**「営業（比留間さん）が対象店舗のGBP状況を5項目で入力 → サイテーションだけAIがWeb検索で精査 → 固有URL＋QRを発行 → オーナー様が"AIの読み解き"として総合点＋項目別スコアを受け取る」**
＝ カンリー無料診断PDFのインタラクティブ版。教育型リード資産。

### V2からの本質的な転換
- V2診断＝オーナー本人が12問を自己申告。
- **V3診断＝営業が5項目を入力して発行 → オーナーは結果を受け取る**（主役が逆転）。

---

## 1. 5項目・配点（カンリー基準・確定）

| 項目 | 配点 | サブ項目 | 入力 |
|---|---|---|---|
| プロフィール | 30 | カテゴリ／メニュー／予約ボタン／リンク | 手入力 |
| 写真 | 15 | 総枚数／オーナー提供枚数／最新写真 | 手入力 |
| クチコミ・返信 | 20 | 件数／評価／最新クチコミ／返信率 | 手入力 |
| 新着投稿 | 15 | 投稿数／週2回以上の継続 | 手入力 |
| サイテーション | 20 | NAP一致／掲載媒体／SNS活用 | 入力→AI精査 |
| **合計** | **100** | | |

- 出典: `カンリーMDファイル/無料診断フォーマット/テンプレート.html`（基本情報30/コンテンツ20/写真15/クチコミ20/投稿15）を5項目に写像。
- **配点は管理画面から調整可能**にする（定数化しない）。
- サブ項目→サブ点を積み上げ→項目点→総合点。「週2回以上」等の閾値は採点ルール化。

---

## 2. アーキテクチャ（V2のレイヤーを踏襲）

```
app/            ⑦ ルーティング・組み立て（page = features を並べる / API = 薄い窓口）
features/       ⑤ 機能セクション（onboarding / diagnosis / consult / guide / result[新] / issue[新]）
components/ui/  ⑥ 共通UI部品（Reactのみ・データを知らない）
design/         ⑧ デザイントークン（見た目の唯一の源）
lib/ai lib/store ③④ AI層・永続化層（Redis / invite）
lib/domain/     ② ロジック層（純関数・テスト対象）＝採点エンジンはここ
content/        ① コンテンツ層（純データ）＝診断項目・採点基準・マニュアル本文
```

依存は下から上へ。`content/` `lib/domain/` は React を import しない（テスト可能に保つ）。

---

## 3. 実装セクション（section-by-section・常に動く状態を保つ）

### S1. 採点エンジン（lib/domain・純関数・TDD）
- `content/diagnosis-v3.ts`：5項目×サブ項目の定義（選択肢→スコア、閾値）。配点は設定から注入。
- `lib/domain/score.ts`：入力→項目点→総合点→ランク（S/A/B/C/D）→改善余地(max-score)。
- `lib/domain/tier.ts`：**満点比の5段階配色**（>=0.8 teal / >=0.6 tealgreen / >=0.4 amber / >=0.2 orange / else red）。内部基準（UIに"5分割"の語は出さない）。
- **テスト必須**（score.test.ts / tier.test.ts）。

### S2. 診断結果ページ（features/result・オーナー様が受け取る／承認モックv6.1準拠）
- 総合点＝**4色オーブ**（ブランド署名）＋カウントアップ＋ランクバッジ（濃紺＋白フチ＋アンバー）。
- 5項目＝**カードからはみ出すリング型スコア**（白中心＋発光＋円弧フィル）を1画面に。tier色で自動配色。
- **長押し（約450ms）で「採点基準×現状」の詳細シート**（バウンド＋スタッガー＋発光パルス）。PCクリックはフォールバック。
- AI検索での見え方＝1行ストリップ（診断コンサル憲法の必須枠）。
- 文言ルール：**事実断定しない**（「全国13万店舗基準」「同業より+6点」等は使わない／「改善の余地があります」）。**「お客様/オーナー様」**表記。
- 憲法遵守：診断＝"評価"（できている/足りない/直すとどうなるか）。具体手順は相談へ。素人向け言葉（専門語NG／「レバー」不使用）。

### S3. 営業入力＋発行（features/diagnosis[営業モード] / features/issue）
- 5項目の入力フォーム（選択チップ）。サイテーションは URL入力→**AIで精査**（`/api/lookup` 拡張：NAP一致・掲載媒体・SNS活用を返し**採点案**を提示、最終は人が微調整）。
- 「URL/QR発行」→ 発行中プログレス→**発行完了画面でQR＋固有URL＋共有**（QRは営業側のみ。オーナー結果画面には出さない）。
- 診断レコードを Redis（`lib/store`）に保存し、`/d/<slug>` の固有URLで再閲覧可能に。

### S4. オンボーディング（features/onboarding・重要変更）
- **初回ログイン時のダッシュボード表示は廃止**。
- **URL来訪時は「使い方マニュアル」をスクロールで見る画面から開始** → 見終わったら **相談 / 診断結果** へ。
- マニュアル本文は `content/manual.ts`（`maplab_v2/docs/manual.html` を素材に）。

### S5. 相談（features/consult）＝V2踏襲＋AI応答強化
- 診断結果を文脈に、最初の3手・今日やることを対話で。00_AI憲法＋10_診断コンサル憲法を厳守。
- （将来）ストリーミング応答は余力で。

### S6. 管理画面（app/admin）
- 配点（重み）の調整、発行済み診断の一覧、招待/使用状況。V2 admin を拡張。

---

## 4. 変えない（非スコープ / 封印）
- インフラ（Redis / Vercel / Gemini）はそのまま。
- AI憲法（00）は不可侵。診断コンサル憲法（10）に従う。
- Googleの4色は「集客の4つの力」＋ブランド署名（総合オーブ）専用。**項目スコアの5段階配色には使わない**（tier色は別スケール）。

---

## 5. デザイン
- 承認モック（v6.1）: https://claude.ai/code/artifact/02b8f7ee-5e9f-4749-a3e1-b93a410d85e7
- `design/tokens.css` `tokens.ts` `STYLEGUIDE.md`（V2から継承）。ライト・モバイル単一ルック。角丸16・影2段・ティール主役。
- 担当: `maplab-design-lead`（各セクションのUIゲートでレビュー）。

---

## 6. 作業順序（推奨）
S1（採点エンジン＋テスト）→ S2（結果ページ＝承認済みで最短で"見える"）→ S3（入力＋発行）→ S4（オンボーディング）→ S5（相談）→ S6（管理）。
各セクション：実装→動作確認→次へ。常に動く状態を保つ。

---

## 7. 環境メモ
- git 利用可 / gh CLI 未インストール（push は git 直・要オーナー承認）。
- 起点コピー: 2026-09-16 に maplab_v2 のソースを本リポジトリへ移植済み（node_modules/.next/.vercel 除く）。
- 詳細要件: `.company/secretary/notes/maplab-v3-requirements.md`。
